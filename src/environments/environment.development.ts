export const environment = {
  production: false,
  apiUrl: '/gw',
  // Enquanto os endpoints reais do TDM não estão disponíveis, o CatalogService
  // retorna dados mockados. Bastar mudar para `false` quando o gateway estiver pronto.
  useMockData: true,
};
